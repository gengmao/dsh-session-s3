# dsh-session peers include unpublished @deepseek-ai/dsh-type-meta.
# Inside a DSH profile that package exists as a workspace dep; for a
# standalone clone, skip the 404 rather than failing npm install.
legacy-peer-deps=true
